'use strict';

const commonUrl = 'https://api.avgle.com/v1';
const pathArr = ['categories', 'collections', 'videos'];
const match = ['all', 'search', 'jav', 'video'];

module.exports = { commonUrl, pathArr, match };
